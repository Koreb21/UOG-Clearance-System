package com.uog.clearance.clearance.model;

public enum ClearanceCheckStatus {
    PENDING,
    IN_REVIEW,
    AWAITING_FINANCE,
    PAID_PENDING_DEPARTMENT_APPROVAL,
    FLAGGED,
    FAILED,
    CLEARED
}
